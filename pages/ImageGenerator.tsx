import React from 'react';

// This component is not currently used or linked anywhere in the app.
// Gulling the component to reduce unused JavaScript code.
export const ImageGenerator: React.FC = () => null;
