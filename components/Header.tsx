import React from 'react';

// This component is unused in the current application.
// It has been replaced by the component in `BottomNav.tsx`.
// Gulling the component to reduce unused JavaScript code.
export const BottomNav: React.FC = () => null;
